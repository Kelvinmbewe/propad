import { ColorSwatches } from './ColorSwatches';

export const metadata = {
  title: 'Aurora Foundations – Colors',
};

# Colors

Aurora ships with a gradient driven palette tuned for WCAG AA contrast. Each swatch shows the compliant ratios for dark and light surfaces.

<ColorSwatches />

> Gradient angle tolerance: 135° ± 15°.
